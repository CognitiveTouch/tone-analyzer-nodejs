---
declared-services:
  tone-analyzer-service:
    label: tone_analyzer
    plan: beta
applications:
- services:
  - tone-analyzer-service
  name: ee-ff
  command: npm start
  path: .
  instances: 2
  memory: 512MB
